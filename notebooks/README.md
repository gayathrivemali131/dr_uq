# Notebooks

Notebooks in this directory are **exploratory only**. They are never a source of reported
results. Every number that appears in a table, figure or the paper is produced by
`scripts/evaluate.py` (and aggregated by `dr_uq/evaluation/report.py`) from a named Hydra
config and a git commit hash. Notebooks are git-ignored (`*.ipynb`).
